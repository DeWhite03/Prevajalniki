fun putint(x: int): void
fun putchar(c: char): void

fun main(): int =
    let 
        var i: int
    in
        i = 0,
        while i <= 15 do
            printInt(i, "g"),
            #putint(f(i)),
            i = i + 1
        end
    end,
    return 0

fun f(i: int): int =
    return i + 1

fun printInt( x: int, msg: ^char): void =
    putint(f(x)),
    putchar('\0x0A')